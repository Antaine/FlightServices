apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flightservice.fullname" . }}-test-connection"
  labels:
    {{- include "flightservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flightservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
